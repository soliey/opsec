[package]
name = "consent"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Auditable mutual-consent handshake for the remote assistance app. No capture, input, or transport logic lives here."

[dependencies]
rand = "0.8"

[lints.clippy]
unwrap_used = "warn"
expect_used = "warn"
